# 🔍 Integrate Elasticsearch for Advanced Search

The enterprise repo connects to Elasticsearch for workshop search and geo
queries. Our project relies on in-memory lookups.

Tasks:

- Add Elasticsearch client to the backend.
- Index activities with relevant fields (title, description, schedule).
- Implement a search endpoint that queries ES and returns matching activities.

This allows full-text search and scalability over large datasets.
